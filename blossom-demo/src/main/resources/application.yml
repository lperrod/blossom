blossom:
  security:
    default:
      account:
        password: system
        identifier: system
        enabled: true
spring:
  jpa:
    hibernate:
      ddl-auto: create

  liquibase:
    enabled: false
server:
  port: 8081
